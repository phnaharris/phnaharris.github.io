---
const { title, techs } = Astro.props;
---

<div class="display:block my-5">
  <span class="text-xl m-0 w-full text-skin-accent">{title}</span>
  <ul class="list-disc m-0 columns-3xs">
    {Object.values(techs).map(item => <li>{item}</li>)}
  </ul>
</div>
